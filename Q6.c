#include<stdio.h>
int main()
{
    int num,m = 1;
    printf("Enter a number : ");
    scanf("%d",&num);
    for(int i = i; i<=num;i++)
    {
        m = m*i;
    }
    printf("Factorial  of %d is %d",num,m);
    return 0;
}
